
// arith.pwc

fn Int square(Int x) {
  return x * x;
}

fn Int add_one(Int x) {
  return x + 1;
}

fn Int main() {
  const a = 3;
  var Int b;
  b = add_one(square(a));
  println(b); // 10
  return 0;
}
