
// arith.pwc

fn Int square(Int x) {
  return x * x;
}

fn Int add_one(Int x) {
  return x + 1;
}

fn Int main() {
  var Int a = 3;
  var Int b;
  b = add_one(square(a));
  println(string_from_int(b)); // 10
}
